QUnit.module('curve', function(hooks) {
    hooks.beforeEach(function(){ 
    });
    hooks.afterEach(function(){
        sinon.restore();
    });

    QUnit.test('_initialize', function( assert ) {      
        
    });
});